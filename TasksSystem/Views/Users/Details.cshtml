@model TasksSystem.Models.DecryptedUser

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>User</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DecryptedFirstName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DecryptedFirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DecryptedLastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DecryptedLastName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tasks)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (TasksSystem.Models.Task task in ViewBag.Tasks)
        {
            <tr>
                <td>
                    @task.Title
                </td>
                <td>
                    <a asp-action="TaskDetails" asp-route-id="@task.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
